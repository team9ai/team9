/**
 * Stub placeholder — the real implementation lands in Task 20 of the Wiki
 * (folder9) Integration plan. Keeping the stub's prop contract stable means
 * Task 16's `WikiSubSidebar` wire-up stays untouched when Task 20 ships.
 */
export interface CreateWikiDialogProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
}

export function CreateWikiDialog(_props: CreateWikiDialogProps) {
  return null;
}

# Agent Runtime Configuration

# Server port
PORT=3001

# Storage mode: 'memory' or 'postgres'
# - memory: In-memory storage (data lost on restart)
# - postgres: PostgreSQL persistent storage
STORAGE_MODE=memory

# PostgreSQL Configuration (required if STORAGE_MODE=postgres)
DEBUGGER_DB_URL=postgres://user:password@localhost:5432/debugger

# OpenRouter API Configuration
# Get your API key from https://openrouter.ai/keys
OPENROUTER_API_KEY=your-openrouter-api-key

# Optional: Set referer for OpenRouter
OPENROUTER_REFERER=http://localhost:3000

# Optional: Set title for OpenRouter
OPENROUTER_TITLE=Agent Debugger
